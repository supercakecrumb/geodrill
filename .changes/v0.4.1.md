## v0.4.1 - 2026-07-21
### Fixed
* Seed files now load correctly in the Docker image — seed paths resolve relative to the working directory (/app/seeds) when the source-relative path isn't present, fixing bot and ingest startup in the distroless container (previously they crashed loading seeds/*.yaml).