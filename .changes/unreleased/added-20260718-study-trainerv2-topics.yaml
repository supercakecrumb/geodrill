kind: Added
body: 'Add internal/study.Service.NextExerciseV2/AnswerV2/AnswerText (TrainerV2, architecture §1.6) and Root/Children (TopicService, architecture §5.2): mode-aware exercise generation over the topic Generator registry (single/set/text, rotating the mode choice by the item''s own review count when a topic configures more than one), index/set-canonicalized and free-text grading with the same atomic FSRS-scheduling + tier-recompute transaction AnswerIntro uses, and the /topics tree browser''s subtree progress rollup, locked-tier badge, and language_id-aware tips badge'
