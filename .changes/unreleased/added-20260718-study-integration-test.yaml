kind: Added
body: 'Add the v2 end-to-end integration test (TestV2FullLoop, GEODRILL_TEST_DATABASE_URL-gated): seeds every topic package, then drives IntroSummary -> NextIntro/AnswerIntro across all three outcomes (asserting lifecycle/card/known_at, introductions rows, tier progress rows, and a stale-second-tap guard) -> NextExerciseV2/AnswerV2 correct + wrong + stale (asserting FSRS movement, review rows, graded options) -> AnswerText on a reps-rotated ModeText exercise -> TopicService.Root/Children shape -> daily-cap budget exhaustion -> tier gating, against a real PostgreSQL 18'
